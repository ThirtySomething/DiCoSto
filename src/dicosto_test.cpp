#define CATCH_CONFIG_MAIN
#include <catch2/catch.hpp>
#include "test/Logger_test.h"

/*
int main(void) {
    net::derpaul::dicosto::Logger::logInit();

    net::derpaul::dicosto::Logger::logDebug("main");

    net::derpaul::dicosto::Container* myContainer = new net::derpaul::dicosto::Container();

    DICOSTO_STR cname = myContainer->containerNameGet();

    net::derpaul::dicosto::Logger::logInfo(cname);

    myContainer = new net::derpaul::dicosto::Container();

    cname = myContainer->containerNameSet("oddo");

    net::derpaul::dicosto::Logger::logInfo(cname);

    myContainer = new net::derpaul::dicosto::Container("kurt");

    cname = myContainer->containerNameGet();

    net::derpaul::dicosto::Logger::logInfo(cname);

    net::derpaul::dicosto::Logger::logDebug("ende");

    return 0;
}
*/